/**
 * Class strings shared by more than one screen, so a treatment is decided once.
 */

/**
 * An outline button in the brand colour that fills on hover — the topbar's
 * Get support, the settings page's wizard link, the detail page's back links.
 */
export const BRAND_OUTLINE =
	'border-primary text-primary hover:border-primary hover:bg-primary hover:text-primary-foreground';
